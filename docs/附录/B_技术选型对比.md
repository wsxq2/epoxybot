# 附录B: 技术选型对比

> 关键技术方案对比分析

---

## 1. ROS版本对比

### 1.1 ROS1 vs ROS2 对比

| 对比项 | ROS1 (Noetic) | ROS2 (Humble) | 结论 |
|--------|---------------|---------------|------|
| **发布时间** | 2020年5月 | 2022年5月 | ROS2更新 |
| **支持期限** | 至2025年5月 | 至2027年5月 | ROS2更长 ✅ |
| **操作系统** | Ubuntu 20.04 | Ubuntu 22.04 | ROS2更新 ✅ |
| **通信机制** | TCP/UDP（自研） | DDS（工业标准） | ROS2更可靠 ✅ |
| **多机通信** | 需要配置ROS_MASTER | 去中心化，自动发现 | ROS2更简单 ✅ |
| **实时性** | 软实时 | 支持硬实时（RTOS） | ROS2更好 ✅ |
| **安全性** | 无加密 | 支持DDS Security | ROS2更安全 ✅ |
| **Python支持** | Python 2/3 | Python 3 | ROS2更现代 ✅ |
| **生态成熟度** | 非常成熟 | 快速成长 | ROS1稍胜 |
| **学习曲线** | 较平缓 | 稍陡峭 | ROS1稍简单 |

**最终选择**: **ROS2 Humble** ✅

**理由**:
1. **长期支持**: 2027年5月才停止维护，适合工业项目
2. **分布式通信**: 两台智能平台 + 控制系统 + 远程监控，DDS天然支持
3. **安全性**: 室外作业环境可能有网络安全要求
4. **未来趋势**: ROS2是未来主流，避免技术债务

---

### 1.2 ROS2发行版对比

| 发行版 | Ubuntu版本 | 支持期限 | 是否选择 |
|--------|-----------|----------|---------|
| Foxy Fitzroy | 20.04 | 2023年5月（已停止） | ❌ |
| Galactic Geochelone | 20.04 | 2022年11月（已停止） | ❌ |
| **Humble Hawksbill** | **22.04** | **2027年5月** | ✅ 选择 |
| Iron Irwini | 22.04 | 2024年11月 | ❌ 支持期短 |
| Jazzy Jalisco | 24.04 | 2029年5月 | ❌ 太新，生态不成熟 |

**选择**: **Humble Hawksbill**

**理由**:
- LTS版本，5年支持期
- Ubuntu 22.04 LTS配套，稳定性好
- 生态成熟，第三方包齐全（NAV2、MoveIt2等）
- 工业项目首选

---

## 2. 通信方案对比

### 2.1 WIFI vs 有线 vs 4G/5G

| 对比项 | WIFI6 | 有线（光纤/网线） | 4G/5G |
|--------|-------|------------------|-------|
| **带宽** | 3000Mbps | 1000Mbps+ | 4G:150Mbps / 5G:1Gbps |
| **延迟** | <10ms | <1ms | 4G:50ms / 5G:10ms |
| **覆盖范围** | 单路由500m, Mesh 3km | 受限于布线长度 | 无限制（有信号即可） |
| **移动性** | 优秀 | 差（需拖线） | 优秀 |
| **稳定性** | 中（受环境干扰） | 优秀 | 中（受信号强度影响） |
| **成本** | 低（~3000元） | 高（布线成本高） | 中（流量费月付） |
| **部署难度** | 低 | 高（需布线） | 低 |
| **适用场景** | 移动机器人 ✅ | 固定设备 | 备用或远程监控 |

**选择**: **WIFI6为主，4G/5G为备用** ✅

**方案**:
1. **推荐方案**: WIFI6 (wavlink AERIAL HD9)
   - 主要工作通信
   - 单路由覆盖500m
   - Mesh扩展至3km（2~3台级联）
   
2. **备用方案**: 4G/5G路由器
   - WIFI6信号不稳定时切换
   - 远程监控备份链路
   - 按需采购（可先不买）

3. **不选有线**: 机器人移动作业，拖线不现实

---

### 2.2 WIFI频段对比

| 频段 | 带宽 | 覆盖范围 | 抗干扰性 | 穿墙能力 | 建议 |
|------|------|----------|----------|----------|------|
| 2.4GHz | 574Mbps | 远（~150m） | 差（常见干扰） | 强 | 备用 |
| **5GHz** | **2402Mbps** | **中（~100m）** | **优秀** | **中** | **主用** ✅ |

**选择**: **5GHz优先，2.4GHz备用**

**理由**:
- 室外环境干扰较少，5GHz性能更好
- 带宽更高，满足视频流传输
- 2.4GHz作为后备，增强覆盖

---

## 3. 定位导航方案对比

### 3.1 SLAM算法对比

| 算法 | 传感器 | 精度 | 实时性 | 成熟度 | 是否选择 |
|------|--------|------|--------|--------|---------|
| **FAST-LIVO2** | **激光+IMU+相机** | **高** | **高** | **较新（2023）** | ✅ **场景建模** |
| Cartographer | 激光+IMU | 高 | 中 | 成熟 | ❌ |
| LOAM | 激光 | 高 | 低 | 成熟 | ❌ |
| ORB-SLAM3 | 相机+IMU | 中 | 高 | 成熟 | ❌ |

**选择**: **FAST-LIVO2**（已确定）

**理由**:
1. **多传感器融合**: 激光+IMU+相机，精度高
2. **场景建模**: 生成高质量3D模型，适合作业规划
3. **适应性强**: 室外光线变化大，融合方案更鲁棒
4. **已验证**: 经过初步验证，具备可行性
5. **无备选**: 已确认使用，不考虑其他SLAM算法

**部署方式**:
- 可插拔套件：1个激光雷达 + 1个IMU + 1个相机
- 安装位置：侧墙平台大臂末端
- 使用场景：侧墙平台走一圈进行全场景建模
- ROS1 Noetic环境（FAST-LIVO2暂无ROS2版本）
- Docker容器隔离
- 通过文件传输PCD点云给ROS2系统

---

### 3.2 导航框架对比

| 框架 | 适用ROS版本 | 功能完整性 | 成熟度 | 是否选择 |
|------|-------------|-----------|--------|---------|
| **NAV2** | **ROS2** | **完整** | **成熟** | ✅ **选择** |
| move_base | ROS1 | 完整 | 非常成熟 | ❌ ROS版本不匹配 |
| TEB Local Planner | ROS1/2 | 局部规划优秀 | 成熟 | ❌ 可作为NAV2插件 |

**选择**: **NAV2**

**理由**:
- ROS2官方导航框架
- 功能完整：全局规划、局部规划、恢复行为
- 插件化设计，易扩展
- 社区活跃，文档完善

**配置方案**:
```yaml
global_planner: "GridBased"  # 栅格地图全局规划
local_planner: "DWB"         # Dynamic Window Approach
controller: "RegulatedPurePursuit"  # 跟踪控制器
```

---

## 4. AI检测算法对比

### 4.1 目标检测算法对比

| 算法 | 发布时间 | 精度 | 速度 | 模型大小 | 是否选择 |
|------|----------|------|------|----------|---------|
| Faster R-CNN | 2015 | 高 | 慢（5fps） | 大 | ❌ |
| SSD | 2016 | 中 | 中（20fps） | 中 | ❌ |
| YOLO v5 | 2020 | 中 | 快（60fps） | 小 | ❌ |
| YOLO v8 | 2023 | 高 | 快（80fps） | 小 | ⚠️ 备选 |
| **YOLO v12** | **2024** | **最高** | **最快（100fps）** | **小** | ✅ **选择** |

**选择**: **YOLO v12**

**理由**:
1. **最新算法**: 2024年发布，精度和速度均优于前代
2. **小缺陷检测**: 0.1mm缺陷需要高精度模型
3. **实时性**: 100fps满足实时检测需求（实际5-10fps即可）
4. **易部署**: PyTorch模型，可转TensorRT加速

**性能验证**:
- 31MP图像: 6480×4860
- YOLO输入: 640×640（缩放）
- 推理时间: ~20ms（TensorRT优化后）
- 实际FPS: 1000/20 = 50fps（满足需求 ✅）

---

### 4.2 模型训练方案

| 方案 | 优点 | 缺点 | 是否选择 |
|------|------|------|---------|
| **从头训练** | 可完全自定义 | 需要大量标注数据（>10000张） | ❌ |
| **迁移学习（COCO预训练）** | 数据需求少（~1000张） | 可能对小缺陷不敏感 | ⚠️ 备选 |
| **半监督学习** | 结合少量标注+大量无标注 | 技术复杂 | ❌ |
| **数据增强 + 迁移学习** | 平衡效果和成本 | - | ✅ **选择** |

**选择方案**: 
1. COCO预训练模型微调
2. 收集1000~2000张室外侧墙和底板缺陷图像
3. 数据增强：旋转、缩放、亮度调整、添加噪声
4. 训练50~100 epochs

---

## 5. 数据存储架构

### 5.1 数据分类与存储策略

本项目采用**混合存储架构**，针对不同类型数据选择最适合的存储方案：

```
┌─────────────────────────────────────────────────────────┐
│                    数据存储架构                          │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  ┌────────────┐      ┌────────────┐      ┌───────────┐ │
│  │  SQLite3   │      │   MySQL    │      │ InfluxDB  │ │
│  │  (嵌入式)   │      │  (关系型)   │      │  (时序)   │ │
│  └────────────┘      └────────────┘      └───────────┘ │
│       ▲                   ▲                    ▲        │
│       │                   │                    │        │
│  ROS Bag数据          业务数据              实时监控数据   │
│  • 激光雷达           • 作业记录            • CPU/内存    │
│  • 相机图像           • 缺陷档案            • 温度/湿度    │
│  • IMU数据            • 厚度检测            • 位置/速度    │
│  • 导航路径           • 用户权限            • WIFI信号    │
│                                                          │
│  特点：               特点：                特点：        │
│  • 临时存储(1周)      • 永久存储            • 自动降采样   │
│  • 本地访问           • 远程访问            • 高频写入    │
│  • 便于传输           • 复杂查询            • 实时面板    │
└─────────────────────────────────────────────────────────┘
```

---

### 5.2 SQLite3 - ROS Bag存储

**用途**: ROS2传感器数据录制（rosbag2默认后端）

**优点**:
- ROS2原生支持，零配置
- 单文件存储，便于归档和传输
- 本地读写性能高
- 不需要数据库服务进程

**存储内容**:
- 激光雷达点云（/scan, 10Hz）
- 相机图像（/camera/image, 30fps）
- IMU数据（/imu, 100Hz）
- 导航路径（/nav/path, 1Hz）

**数据管理**:
- 保留周期：7天（定期清理旧bag）
- 单个bag大小：~2GB（录制30分钟）
- 存储路径：`/data/rosbags/YYYY-MM-DD_HH-MM-SS.db3`

**不涉及业务表设计** ✅

---

### 5.3 MySQL - 业务数据管理

**用途**: 作业记录、缺陷档案、厚度检测等结构化业务数据

| 对比项 | MySQL | PostgreSQL | MongoDB |
|--------|-------|-----------|---------|
| **成熟度** | 非常成熟 | 成熟 | 成熟 |
| **性能** | 高（读优化） | 高（写优化） | 高 |
| **复杂查询** | 优秀 | 优秀 | 弱 |
| **远程访问** | 原生支持 | 原生支持 | 原生支持 |
| **学习曲线** | 平缓 | 稍陡 | 中等 |
| **是否选择** | ✅ **选择** | ❌ 功能过强 | ❌ 不适合结构化数据 |

**选择**: **MySQL 8.0** ✅

**理由**:
1. **远程访问**: 办公室电脑可查询现场作业数据
2. **复杂查询**: 支持JOIN、子查询、视图、存储过程
3. **数据分析**: 可对接BI工具（缺陷统计、作业效率分析）
4. **生态成熟**: 工具链完善（Navicat、phpMyAdmin、Grafana）
5. **成本低**: 开源免费，社区版功能完整

**核心表设计**（详细设计见接口文档）:

```sql
-- 作业记录表
CREATE TABLE jobs (
    job_id INT PRIMARY KEY AUTO_INCREMENT,
    start_time DATETIME,
    end_time DATETIME,
    work_area VARCHAR(50),  -- 侧墙/底板
    operator VARCHAR(50),
    status ENUM('in_progress', 'completed', 'failed')
);

-- 缺陷数据表
CREATE TABLE defects (
    defect_id INT PRIMARY KEY AUTO_INCREMENT,
    job_id INT,
    detect_time DATETIME,
    position_x FLOAT,
    position_y FLOAT,
    defect_type VARCHAR(50),  -- 裂缝/孔洞/破损
    size_mm FLOAT,
    image_path VARCHAR(255),
    repair_status ENUM('pending', 'repaired', 'ignored'),
    FOREIGN KEY (job_id) REFERENCES jobs(job_id)
);

-- 厚度检测表
CREATE TABLE thickness_data (
    id INT PRIMARY KEY AUTO_INCREMENT,
    job_id INT,
    measure_time DATETIME,
    position_x FLOAT,
    position_y FLOAT,
    thickness_mm FLOAT,
    is_qualified BOOLEAN,
    FOREIGN KEY (job_id) REFERENCES jobs(job_id)
);

-- 用户权限表
CREATE TABLE users (
    user_id INT PRIMARY KEY AUTO_INCREMENT,
    username VARCHAR(50) UNIQUE,
    password_hash VARCHAR(255),
    role ENUM('admin', 'operator', 'viewer')
);
```

**部署方案**:
- 容器化部署：Docker MySQL 8.0
- 数据持久化：挂载 `/data/mysql` 到宿主机
- 网络访问：`3306`端口（内网开放）

---

### 5.4 InfluxDB - 实时监控数据（可选）

**用途**: 高频时序数据采集与监控

**为什么需要InfluxDB**:
- MySQL/SQLite **不擅长时序数据**：
  - 无自动降采样（需手动删除旧数据）
  - 时间范围查询性能差
  - 无聚合函数（平均值、最大值需要手写SQL）

**适用场景**:
- 系统性能监控（CPU、内存、温度）
- 机器人实时状态（位置、速度、电压）
- 环境数据（温湿度、WIFI信号强度）

**核心优势**:
1. **高吞吐写入**: 百万级points/秒
2. **自动降采样**: 1秒数据保留1周，1分钟数据保留1年
3. **时间查询语法**: `SELECT * FROM cpu WHERE time > now() - 1h`
4. **Grafana集成**: 一键配置实时监控面板

**部署方案**（可选）:
- 容器化：Docker InfluxDB 2.x
- 数据源：ROS2节点定期发布系统指标
- 可视化：Grafana实时面板

**优先级**: ⚠️ **中等**（锦上添花，非核心功能）

---

### 5.5 存储架构总结

| 数据库 | 是否必须 | 用途 | 增加工作量 | 备注 |
|--------|---------|------|-----------|------|
| **SQLite3** | ✅ 必须 | ROS bag录制 | 0（ROS2自带） | 无需表设计 |
| **MySQL** | ✅ **强烈建议** | 业务数据管理 | +3天 | 需要表设计 |
| **InfluxDB** | ⚠️ 可选 | 实时监控 | +2天 | 视预算和时间 |

**最终建议**:
1. SQLite（必须）+ MySQL（强烈建议）作为**基础架构**
2. InfluxDB作为**增强功能**，如有时间和预算可实施
3. MySQL是刚需：远程查询缺陷报告、作业统计是业务必需功能

---

## 6. 编程语言对比

### 6.1 主语言选择

| 语言 | 性能 | 生态 | 学习曲线 | 适用模块 | 占比 |
|------|------|------|----------|----------|------|
| **C++** | **极高** | **ROS2核心** | **陡** | **SLAM、导航、控制** | **70%** ✅ |
| **Python** | **中** | **AI、工具丰富** | **平缓** | **AI检测、数据处理、工具** | **30%** ✅ |
| Rust | 高 | 小众 | 陡 | - | ❌ 不选 |

**选择**: **C++ 为主，Python 为辅**

**分工**:
- **C++ 模块**:
  - 激光雷达驱动
  - 相机驱动
  - SLAM建图
  - NAV2导航
  - PLC通信（Modbus）
  - 实时控制

- **Python 模块**:
  - YOLO缺陷检测
  - 数据分析工具
  - Web服务（可选）
  - 测试脚本

---

### 6.2 C++标准选择

| 标准 | 发布时间 | 主要特性 | 是否选择 |
|------|----------|----------|---------|
| C++11 | 2011 | auto, lambda, 智能指针 | ❌ 过旧 |
| C++14 | 2014 | 泛型lambda | ❌ |
| **C++17** | **2017** | **std::optional, 结构化绑定** | ✅ **选择** |
| C++20 | 2020 | Concepts, 协程 | ❌ GCC支持不完整 |

**选择**: **C++17**

**理由**:
- ROS2 Humble默认支持C++17
- GCC 11完整支持
- 现代特性足够（智能指针、lambda、optional）
- 不激进，稳定性好

**编码规范**: **Google C++ Style Guide**

---

## 7. 开发工具对比

### 7.1 IDE选择

| IDE | 优点 | 缺点 | 是否选择 |
|-----|------|------|---------|
| **VS Code** | 轻量、插件丰富、免费 | - | ✅ **推荐** |
| CLion | 强大的C++支持 | 收费 | ⚠️ 可选 |
| Qt Creator | Qt项目集成好 | C++功能稍弱 | ⚠️ UI开发可选 |
| Vim/Emacs | 高效（高手） | 学习曲线陡 | ❌ |

**推荐**: **VS Code + C++ Extension + ROS Extension**

---

### 7.2 调试工具

| 工具 | 用途 | 是否使用 |
|------|------|---------|
| **GDB** | C++调试 | ✅ |
| **rqt_console** | ROS日志查看 | ✅ |
| **RViz2** | 3D可视化 | ✅ |
| **rqt_graph** | 节点拓扑图 | ✅ |
| **ros2 topic echo** | Topic监控 | ✅ |

---

## 8. 版本控制与CI/CD

### 8.1 版本控制

| 工具 | 是否使用 |
|------|---------|
| **Git** | ✅ 版本控制 |
| **GitHub/GitLab** | ✅ 代码托管 |

**分支策略**: Git Flow
- `main`: 稳定版本
- `develop`: 开发版本
- `feature/*`: 功能分支
- `hotfix/*`: 紧急修复

---

### 8.2 CI/CD

| 工具 | 用途 | 是否使用 |
|------|------|---------|
| GitHub Actions | 自动化测试、编译 | ⚠️ 可选 |
| Docker | 环境隔离 | ✅ FAST-LIVO2容器 |

---

## 9. 总结

### 9.1 最终技术栈

```
操作系统: Ubuntu 22.04 LTS
机器人框架: ROS2 Humble Hawksbill
通信中间件: FastDDS
编程语言: C++17 (主) + Python 3.10 (辅)
编码规范: Google C++ Style Guide

核心算法:
├── SLAM建图: FAST-LIVO2 (ROS1 Docker)
├── 导航: NAV2 (ROS2)
└── AI检测: YOLO v12

通信方案: WIFI6 (主) + 4G/5G (备)

数据存储:
├── SQLite3: ROS bag录制（必须）
├── MySQL 8.0: 业务数据管理（强烈建议）
└── InfluxDB 2.x: 实时监控（可选）

开发工具: VS Code + GDB + RViz2
```

---

**版本**: 1.0  
**最后更新**: 2026-05-19  
**维护者**: 王志强
