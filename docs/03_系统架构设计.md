# 系统架构设计

> 软件系统的核心设计文档

---

## 1. 架构概览

### 1.1 总体架构

```mermaid
graph TB
    subgraph "交互层 - 用户界面"
        UI1[远程可视化管理平台<br/>PC / Ubuntu 22.04]
        UI2[控制系统<br/>平板电脑 / Ubuntu 22.04]
    end
    
    subgraph "应用层 - 业务逻辑"
        direction LR
        APP[工控机软件<br/>Ubuntu 22.04 + ROS2 Humble]
        
        subgraph "核心模块"
            M1[场景建模]
            M2[定位导航]
            M3[缺陷检测]
            M4[作业规划]
            M5[机械臂规划]
            M6[手动控制]
            M7[自动控制]
            M8[状态监测]
        end
    end
    
    subgraph "硬件层 - 设备控制"
        HW1[PLC<br/>运动控制]
        HW2[底盘导航雷达×2<br/>导航+避障]
        HW2B[FAST-LIVO2套件<br/>1雷达+1IMU+1相机<br/>（场景建模）]
        HW3[缺陷检测相机×1<br/>MV-CH650-90TM]
        HW4[编码器<br/>里程计]
        HW5[激光测距仪×10<br/>厚度检测]
    end
    
    UI1 -.DDS通信.-> APP
    UI2 -.DDS通信.-> APP
    
    APP --> HW1
    APP --> HW2
    APP --> HW2B
    APP --> HW3
    APP --> HW4
    APP --> HW5
```

### 1.2 软件清单

| 软件名称 | 运行平台 | 开发框架 | 主要功能 | 适用设备 |
|----------|----------|----------|----------|----------|
| 远程可视化管理平台 | PC (Ubuntu 22.04) | Qt + ROS2 | 远程监控、进度可视化、远程制动 | 所有设备 |
| 控制系统 | 平板电脑 (Ubuntu 22.04) | Qt + ROS2 | 手动/自动控制、状态监测 | 所有设备 |
| 工控机软件 | 工控机 (Ubuntu 22.04) | ROS2 Humble | SLAM、AI检测、导航控制 | 侧墙、底板平台 |

**注意**：环氧砂浆设备仅需遥控操作，不需要工控机和智能功能。

### 1.3 技术栈

```mermaid
graph LR
    subgraph "操作系统层"
        OS[Ubuntu 22.04 LTS]
    end
    
    subgraph "中间件层"
        MW1[ROS2 Humble]
        MW2[DDS - FastDDS]
        MW3[Qt 5.15]
    end
    
    subgraph "算法库"
        ALG1[FAST-LIVO2<br/>场景建模]
        ALG2[NAV2<br/>导航]
        ALG3[YOLO v12<br/>检测]
    end
    
    subgraph "编程语言"
        LANG1[C++ 17]
        LANG2[Python 3.10]
    end
    
    OS --> MW1
    MW1 --> MW2
    MW1 --> ALG1
    MW1 --> ALG2
    MW1 --> ALG3
```

---

## 2. 软件架构

### 2.1 工控机软件架构

#### 2.1.1 模块划分

```mermaid
graph TB
    subgraph "数据采集层"
        D1[激光雷达驱动]
        D2[相机驱动]
        D3[IMU驱动]
        D4[编码器驱动]
        D5[PLC通信]
    end
    
    subgraph "感知层"
        P1[SLAM建图模块]
        P2[缺陷识别模块]
        P3[厚度检测模块]
        P4[定位模块]
    end
    
    subgraph "决策层"
        DC1[作业规划模块]
        DC2[路径规划模块]
        DC3[机械臂规划模块]
    end
    
    subgraph "控制层"
        C1[手动控制模块]
        C2[自动控制模块]
        C3[安全管理模块]
    end
    
    subgraph "服务层"
        S1[数据存储服务]
        S2[状态发布服务]
        S3[工艺配方服务]
    end
    
    D1 & D2 & D3 & D4 --> P1 & P2 & P3 & P4
    P1 & P2 & P3 & P4 --> DC1 & DC2 & DC3
    DC1 & DC2 & DC3 --> C1 & C2
    C1 & C2 --> D5
    C2 & C3 --> S1 & S2 & S3
```

#### 2.1.2 ROS2 节点架构

| 节点名称 | 功能 | 订阅Topic | 发布Topic |
|----------|------|-----------|-----------|
| lidar_driver_node | 激光雷达数据采集 | - | /scan, /pointcloud |
| camera_driver_node | 相机数据采集 | - | /camera/image_raw |
| slam_node | SLAM建图与定位 | /scan, /imu, /odom | /map, /pose |
| defect_detection_node | 缺陷检测 | /camera/image_raw | /defects |
| navigation_node | 导航规划 | /map, /pose | /cmd_vel |
| control_node | 运动控制 | /cmd_vel, /manual_cmd | /plc/command |
| plc_bridge_node | PLC通信桥接 | /plc/command | /plc/status |

---

### 2.2 控制系统软件架构

#### 2.2.1 界面架构

```mermaid
graph TB
    subgraph "UI层"
        UI1[主界面]
        UI2[手动控制界面]
        UI3[自动控制界面]
        UI4[状态监测界面]
        UI5[工艺配方界面]
        UI6[设置界面]
    end
    
    subgraph "业务逻辑层"
        BL1[控制指令生成器]
        BL2[状态数据解析器]
        BL3[配置管理器]
    end
    
    subgraph "通信层"
        COM1[DDS发布器]
        COM2[DDS订阅器]
    end
    
    UI1 --> UI2 & UI3 & UI4 & UI5 & UI6
    UI2 & UI3 --> BL1
    UI4 --> BL2
    UI5 & UI6 --> BL3
    
    BL1 --> COM1
    COM2 --> BL2
```

#### 2.2.2 控制流程

```mermaid
sequenceDiagram
    participant 用户
    participant 控制UI
    participant DDS
    participant 工控机
    participant PLC
    
    用户->>控制UI: 点击手动控制按钮
    控制UI->>DDS: 发布控制指令
    DDS->>工控机: 转发指令
    工控机->>PLC: Modbus TCP指令
    PLC->>工控机: 返回状态
    工控机->>DDS: 发布状态更新
    DDS->>控制UI: 更新UI显示
    控制UI->>用户: 显示执行结果
```

---

### 2.3 远程可视化管理平台架构

#### 2.3.1 功能模块

```mermaid
graph LR
    subgraph "监控模块"
        M1[实时状态监控]
        M2[视频流监控]
        M3[传感器数据监控]
    end
    
    subgraph "控制模块"
        C1[远程制动]
        C2[参数调整]
    end
    
    subgraph "可视化模块"
        V1[3D场景展示]
        V2[进度展示]
        V3[数据图表]
    end
    
    subgraph "管理模块"
        MG1[用户管理]
        MG2[权限管理]
        MG3[日志管理]
    end
```

---

## 3. 通信架构

### 3.1 DDS通信机制

#### 3.1.1 Topic设计

| Topic名称 | 数据类型 | QoS | 发布者 | 订阅者 | 频率 |
|-----------|----------|-----|--------|--------|------|
| /robot/state | RobotState | Reliable | 工控机 | 控制系统、管理平台 | 10Hz |
| /robot/cmd | RobotCommand | Reliable | 控制系统 | 工控机 | 事件触发 |
| /robot/emergency_stop | EmergencyStop | Reliable | 控制系统、管理平台 | 工控机 | 事件触发 |
| /sensor/camera/image | Image | BestEffort | 工控机 | 管理平台 | 5Hz |
| /sensor/lidar/scan | LaserScan | Reliable | 工控机 | SLAM节点 | 10Hz |
| /defects | DefectArray | Reliable | 缺陷检测节点 | 管理平台 | 1Hz |
| /progress | Progress | Reliable | 工控机 | 控制系统、管理平台 | 1Hz |

#### 3.1.2 QoS配置

```yaml
# 控制指令 - 高可靠性
control_qos:
  reliability: RELIABLE
  durability: TRANSIENT_LOCAL
  deadline: 100ms
  lifespan: 1s

# 传感器数据 - 高吞吐量
sensor_qos:
  reliability: BEST_EFFORT
  durability: VOLATILE
  deadline: 200ms

# 状态数据 - 可靠性优先
state_qos:
  reliability: RELIABLE
  durability: TRANSIENT_LOCAL
  history: KEEP_LAST
  depth: 10
```

### 3.2 组网方案

#### 3.2.1 WIFI6方案（推荐）

```mermaid
graph TB
    subgraph "办公区"
        PC[PC<br/>远程管理]
        SWITCH1[交换机]
    end
    
    subgraph "作业区"
        ROUTER[大功率WIFI6路由器<br/>wavlink AERIAL HD9]
        
        subgraph "侧墙机器人"
            PAD1[平板电脑]
            IPC1[工控机]
            PLC1[PLC]
        end
        
        subgraph "底板机器人"
            PAD2[平板电脑]
            IPC2[工控机]
            PLC2[PLC]
        end
        
        subgraph "环氧砂浆机器人"
            PAD3[平板电脑]
            IPC3[工控机]
            PLC3[PLC]
        end
    end
    
    PC -.光纤/网线.-> SWITCH1
    SWITCH1 -.光纤/网线.-> ROUTER
    
    ROUTER -.WIFI6.-> PAD1 & PAD2 & PAD3
    ROUTER -.WIFI6.-> IPC1 & IPC2 & IPC3
    
    IPC1 -.交换机.-> PLC1
    IPC2 -.交换机.-> PLC2
    IPC3 -.交换机.-> PLC3
```

**覆盖范围**: 
- 单路由器: ~500m
- Mesh拓展: 最远可达3km

**带宽分配**:
- 总带宽: 1800Mbps (WIFI6)
- 控制指令: 优先级最高（<1Mbps）
- 传感器数据: 中优先级（~100Mbps/机器人）
- 视频流: 低优先级（~50Mbps/机器人）

#### 3.2.2 4G/5G备用方案

```mermaid
graph TB
    SERVER[服务器<br/>公网]
    
    ROUTER1[4G路由器<br/>侧墙机器人]
    ROUTER2[4G路由器<br/>底板机器人]
    ROUTER3[4G路由器<br/>环氧砂浆机器人]
    
    PC[PC<br/>办公室]
    
    SERVER -.互联网.-> ROUTER1 & ROUTER2 & ROUTER3
    SERVER -.互联网.-> PC
```

**适用场景**: WIFI6信号不稳定时自动切换

---

## 4. 数据架构

### 4.1 数据流图

```mermaid
graph LR
    subgraph "数据源"
        S1[激光雷达]
        S2[相机]
        S3[IMU]
        S4[PLC状态]
    end
    
    subgraph "数据处理"
        P1[SLAM处理]
        P2[AI推理]
        P3[状态解析]
    end
    
    subgraph "数据存储"
        DB[(SQLite数据库)]
        FILE[文件存储]
    end
    
    subgraph "数据展示"
        UI1[控制系统UI]
        UI2[管理平台UI]
    end
    
    S1 --> P1
    S2 --> P2
    S3 --> P1
    S4 --> P3
    
    P1 --> DB & FILE
    P2 --> DB
    P3 --> UI1 & UI2
    
    DB --> UI1 & UI2
```

### 4.2 数据库设计

#### 4.2.1 数据表结构

**作业记录表 (task_record)**
```sql
CREATE TABLE task_record (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    robot_id TEXT NOT NULL,
    task_type TEXT NOT NULL,  -- 'inspect', 'grind', 'spray'
    start_time TIMESTAMP,
    end_time TIMESTAMP,
    status TEXT,  -- 'running', 'completed', 'failed'
    progress REAL
);
```

**缺陷记录表 (defect_record)**
```sql
CREATE TABLE defect_record (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    task_id INTEGER,
    defect_type TEXT,  -- 'crack', 'bubble', 'unevenness'
    position_x REAL,
    position_y REAL,
    position_z REAL,
    severity INTEGER,  -- 1-5
    image_path TEXT,
    detection_time TIMESTAMP,
    FOREIGN KEY(task_id) REFERENCES task_record(id)
);
```

**厚度检测表 (thickness_record)**
```sql
CREATE TABLE thickness_record (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    task_id INTEGER,
    position_x REAL,
    position_y REAL,
    thickness_before REAL,
    thickness_after REAL,
    measurement_time TIMESTAMP,
    FOREIGN KEY(task_id) REFERENCES task_record(id)
);
```

### 4.3 数据存储策略

| 数据类型 | 存储位置 | 保留时长 | 备份策略 |
|----------|----------|----------|----------|
| 传感器原始数据 | 工控机SSD | 7天 | 不备份 |
| 处理后数据（缺陷、厚度） | SQLite数据库 | 永久 | 每日备份到PC |
| 3D模型 | 工控机SSD | 永久 | 手动备份 |
| 系统日志 | 工控机SSD | 30天 | 每周备份 |
| 视频录像 | 工控机SSD | 3天 | 不备份 |

---

## 5. 关键设计

### 5.1 场景建模方案

#### 5.1.1 技术方案
- **算法**: FAST-LIVO2（激光+IMU+视觉融合）
- **部署方式**: 独立模块，ROS1环境
- **数据传输**: 通过scp传输PCD文件

#### 5.1.2 工作流程

```mermaid
sequenceDiagram
    participant 操作员
    participant 建模模块
    participant 激光雷达
    participant 相机
    participant IMU
    
    操作员->>建模模块: 启动建模
    loop 数据采集
        激光雷达->>建模模块: 点云数据
        相机->>建模模块: 图像数据
        IMU->>建模模块: 惯性数据
        建模模块->>建模模块: 实时融合
    end
    建模模块->>建模模块: 生成PCD文件
    建模模块->>操作员: 建模完成
```

### 5.2 定位导航方案

#### 5.2.1 技术方案
- **框架**: ROS2 NAV2
- **定位**: AMCL（自适应蒙特卡洛定位）
- **规划器**: Regulated Pure Pursuit
- **控制器**: DWB (Dynamic Window Approach)

#### 5.2.2 导航流程

```mermaid
graph LR
    A[获取目标点] --> B[全局路径规划]
    B --> C[局部路径规划]
    C --> D[速度控制]
    D --> E[底盘运动]
    E --> F[位姿反馈]
    F --> C
```

### 5.3 缺陷检测方案

#### 5.3.1 技术方案
- **算法**: YOLO v12
- **输入**: 高精度相机图像（31MP）
- **输出**: 缺陷类型、位置、置信度

#### 5.3.2 检测流程

```mermaid
graph LR
    A[采集图像] --> B[预处理]
    B --> C[YOLO推理]
    C --> D[后处理]
    D --> E{是否有缺陷?}
    E -->|是| F[标注缺陷]
    E -->|否| G[记录正常]
    F --> H[存储到数据库]
    G --> H
```

---

## 6. 部署视图

### 6.1 硬件部署

详见 [附录A_硬件选型清单](./附录/A_硬件选型清单.md)

### 6.2 软件部署

#### 6.2.1 工控机软件部署

```
工控机 (宸曜 Nuvo-10003-MH5)
├── Ubuntu 22.04 LTS
├── ROS2 Humble
│   ├── 激光雷达驱动
│   ├── 相机驱动
│   ├── NAV2导航栈
│   └── 自研节点
├── Docker容器
│   └── FAST-LIVO2 (ROS1 Noetic)
├── AI推理引擎
│   └── YOLO v12 (RTX 4060 Ti 16GB加速)
└── SQLite数据库
```

#### 6.2.2 控制系统部署

```
平板电脑 (亿道三防 MES PAD Q122J)
├── Ubuntu 22.04 LTS
├── ROS2 Humble
└── Qt应用程序
    ├── 控制UI
    └── ROS2通信库
```

---

## 7. 接口设计

详见 [04_接口设计文档](./04_接口设计文档.md)

---

## 8. 安全设计

### 8.1 功能安全

```mermaid
graph TB
    subgraph "安全层级"
        L1[硬件急停按钮]
        L2[软件急停指令]
        L3[碰撞检测]
        L4[软限位]
        L5[超时保护]
    end
    
    L1 -.最高优先级.-> STOP[停止所有动作]
    L2 --> STOP
    L3 --> STOP
    L4 --> STOP
    L5 --> STOP
```

### 8.2 信息安全

- **用户认证**: 用户名+密码
- **数据加密**: DDS传输加密（TLS）
- **权限管理**: 基于角色的访问控制（RBAC）
- **审计日志**: 记录所有关键操作

---

## 9. 性能设计

### 9.1 性能指标

| 指标 | 目标值 | 测试方法 |
|------|--------|----------|
| 控制指令延迟 | <100ms | 端到端时延测试 |
| 视频流延迟 | <1s | 网络延迟测试 |
| SLAM更新频率 | 10Hz | 性能分析工具 |
| AI推理速度 | >5FPS | GPU利用率监控 |
| 数据库查询响应 | <100ms | 压力测试 |

### 9.2 性能优化策略

- **多线程处理**: ROS2 MultiThreadedExecutor
- **GPU加速**: YOLO模型TensorRT优化
- **数据压缩**: 图像JPEG压缩传输
- **缓存机制**: 频繁查询数据缓存

---

## 10. 可维护性设计

### 10.1 日志策略

```python
# 日志级别
DEBUG    # 调试信息
INFO     # 正常运行信息
WARNING  # 警告信息
ERROR    # 错误信息
CRITICAL # 严重错误

# 日志格式
[时间] [级别] [模块名] [线程ID] 消息内容
```

### 10.2 故障诊断

```mermaid
graph TB
    A[检测到异常] --> B{是否关键错误?}
    B -->|是| C[紧急停止]
    B -->|否| D[记录日志]
    C --> E[发送告警]
    D --> F{是否可自动恢复?}
    F -->|是| G[执行恢复流程]
    F -->|否| H[人工介入]
    E --> H
```

---

**下一步**: 查看 [04_接口设计文档](./04_接口设计文档.md) 了解详细接口定义
